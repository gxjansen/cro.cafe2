# NPM configuration for this project
# (ARM64 Rollup binary added as optional dependency)
prefer-online=false
prefer-offline=true

# Optimize npm install for production builds
fund=false
audit=false
save-exact=false
package-lock=true
progress=false

# Skip optional dependencies in production when NODE_ENV=production
# omit=optional

# Reduce install noise
loglevel=warn